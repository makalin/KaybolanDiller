# Datasets

Place rare-language parallel corpora here (OPUS, ELAR exports, community contributions).

Expected layout:

```
datasets/
  en-tr/
    train.tsv
    dev.tsv
  en-mes/
    lexicon.json
```

Each TSV file should use tab-separated `source\ttarget` lines.
